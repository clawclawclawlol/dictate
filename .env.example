# Dictate Configuration
# Copy this to .env and customize as needed
# The .env file is ignored by git, so your local settings won't be tracked

# Audio device index (see list at startup, leave empty for system default)
# DICTATE_AUDIO_DEVICE=3

# Output mode: 'type' (default) or 'clipboard'
DICTATE_OUTPUT_MODE=type

# Input language for transcription (e.g., en, pl, es, fr, de, or 'auto' to auto-detect)
DICTATE_INPUT_LANGUAGE=en

# Enable/disable LLM text cleanup (true/false)
DICTATE_LLM_CLEANUP=true

# LLM model choice: 'qwen-1.5b', 'qwen' (default), 'qwen-7b', 'qwen-14b'
DICTATE_LLM_MODEL=qwen

# Enable verbose logging (true/false)
DICTATE_VERBOSE=false
